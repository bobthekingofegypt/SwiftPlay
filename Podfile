# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SwiftPlay' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SwiftPlay
  pod 'SnapKit', '~> 3.2.0'
  pod 'GoogleMaterialDesignIcons', '~> 3.0.0'
  pod 'SwiftDate', '~> 4.0'
  pod 'Mapbox-iOS-SDK', '~> 3.6'

  target 'SwiftPlayTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SwiftPlayUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
